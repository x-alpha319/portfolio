//   Use state hooks allows a button to remain constant upon rerendering,it also returns an array
// e.g const state = useState(). it would need to be imported,
// it would return two values, one for the original value and the other for the updated value
// const[first,second]=useState(6)
// to fetch an input value
//  const [inputValue,setInputValue] = useState("")
// CONDITIONAL RENDERING
// CAN BE DONE USING EITHER SHORT CIRCUITING OR TERNARY OPERATOR
// to change the Port you go to viteReact.config and input server{
//     port: digit of your choice from 3000- 5000

// } to use a router we would need
// Browser Router

// LAYOUT
// a different layout will be created for LayoutDashboard, authentification page and so on
// whatever takes in props would be in the component folder
// UseEffect allows us to run a code when a component renders
